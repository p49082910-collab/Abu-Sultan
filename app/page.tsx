'use client';
import { useEffect } from 'react';
import { useAppStore } from '@/lib/store';
import { Header } from '@/components/Header';
import { ShieldCheck, Zap, Clock3, ArrowLeft, BadgeCheck, WalletCards } from 'lucide-react';
import Link from 'next/link';
import Image from 'next/image';
import { useRouter } from 'next/navigation';

const features = [{ icon: Zap, title: 'تسليم خلال ثوانٍ', text: 'بيانات الدخول أو الكود يظهر لك مباشرة بعد إتمام الشراء.' }, { icon: ShieldCheck, title: 'تجربة موثوقة', text: 'رصيدك محفوظ في محفظة آمنة، والعملية واضحة من البداية.' }, { icon: Clock3, title: 'دعم حاضر', text: 'نحن هنا لمساعدتك في كل خطوة من التصفح حتى التفعيل.' }];
export default function Home() {
  const { user, products, fetchInitialData } = useAppStore(); const router = useRouter();
  useEffect(() => { fetchInitialData(); }, [fetchInitialData]);
  useEffect(() => { if (user) router.push(user.role === 'admin' ? '/admin' : '/store'); }, [user, router]);
  if (user) return null;
  return <><Header /><main className="surface-grid min-h-screen overflow-hidden"><section className="mx-auto grid max-w-7xl items-center gap-12 px-4 pb-20 pt-16 sm:px-6 lg:grid-cols-[1.1fr_.9fr] lg:pb-28 lg:pt-24"><div className="rise-in max-w-2xl"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-primary/30 bg-primary/10 px-3 py-1.5 text-xs text-primary"><BadgeCheck size={15} />اختيارات رقمية بعناية</div><h1 className="text-balance text-4xl font-black leading-[1.2] tracking-tight sm:text-6xl">كل ما تحتاجه،<br /><span className="gold-text">بلمسة واحدة.</span></h1><p className="mt-6 max-w-xl text-pretty text-base leading-8 text-muted-foreground sm:text-lg">متجر أبو سلطان للباقات الرقمية والحسابات الجاهزة. اشترِ بأمان، واستلم بياناتك فوراً دون انتظار.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link href="/auth" className="gold-shimmer inline-flex items-center justify-center gap-2 rounded-xl px-6 py-3.5 font-bold text-primary-foreground">تصفح المتجر <ArrowLeft size={18} /></Link><button onClick={useAppStore.getState().loginAsCustomer} className="rounded-xl border border-border bg-card px-6 py-3.5 font-semibold text-foreground hover:border-primary/60">دخول تجريبي</button></div><div className="mt-10 flex items-center gap-6 text-xs text-muted-foreground"><span className="flex items-center gap-2"><WalletCards size={16} className="text-primary" />محفظة رقمية</span><span className="flex items-center gap-2"><ShieldCheck size={16} className="text-primary" />شراء آمن</span></div></div><div className="relative mx-auto w-full max-w-md"><div className="absolute -inset-10 rounded-full bg-primary/10 blur-3xl" /><div className="relative overflow-hidden rounded-[2rem] border border-primary/30 bg-card p-3 shadow-2xl shadow-primary/10"><Image src="/logo.png" alt="شعار متجر أبو سلطان" width={560} height={560} priority className="w-full rounded-[1.5rem] object-cover" /></div></div></section><section className="border-y border-border/70 bg-card/40"><div className="mx-auto grid max-w-7xl gap-px px-4 sm:px-6 md:grid-cols-3">{features.map(({ icon: Icon, title, text }) => <div key={title} className="flex gap-4 py-8 md:px-6"><div className="flex size-11 shrink-0 items-center justify-center rounded-xl bg-primary/10 text-primary"><Icon size={22} /></div><div><h2 className="font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div></div>)}</div></section><section className="mx-auto max-w-7xl px-4 py-20 sm:px-6"><div className="mb-8 flex items-end justify-between"><div><p className="mb-2 text-sm font-semibold text-primary">الأكثر طلباً</p><h2 className="text-3xl font-black">باقات مختارة لك</h2></div><Link href="/auth" className="text-sm text-muted-foreground hover:text-primary">عرض الكل <ArrowLeft className="mr-1 inline" size={15} /></Link></div><div className="grid gap-5 md:grid-cols-3">{products.slice(0, 3).map((product) => <article key={product.id} className="glow-border rounded-2xl bg-card p-4"><div className="relative mb-4 aspect-[4/3] overflow-hidden rounded-xl bg-muted"><Image src={product.image_url || '/logo.png'} alt={product.name} fill className="object-cover" /></div><h3 className="font-bold">{product.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-muted-foreground">{product.description}</p><div className="mt-5 flex items-center justify-between"><span className="text-xl font-black text-primary">{product.sale_price || product.price} <small className="text-xs font-normal">ر.س</small></span><Link href="/auth" className="rounded-lg bg-secondary px-3 py-2 text-xs font-bold hover:bg-primary hover:text-primary-foreground">التفاصيل</Link></div></article>)}</div></section></main></>;
}
